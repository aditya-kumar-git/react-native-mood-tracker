import { StyleSheet } from "react-native";

export default StyleSheet.create({
  BackContainer: {
    backgroundColor: "red",
    height: 50,
    width: 50,
    borderRadius: 50,
    justifyContent: "center",
    alignItems: "center",
  },
});
