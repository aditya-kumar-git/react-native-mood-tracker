import { StyleSheet } from "react-native";

export default StyleSheet.create({
  ButtonContainer: {
    padding: 20,
  },
  Button: {
    color: "red",
    paddingVertical: 20,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 60,
  },
  ButtonText: {
    fontWeight: "bold",
    fontSize: 16,
  },
});
